function greet(input){
   
  let hour=Number(input[0]);
  let day=input[1];
  
  let isOpen="open"

  if (hour<10||hour>18) {
    isOpen="closed"
  }
  
  if (day==="Sunday") {
    isOpen="closed"
  }
  
  console.log(isOpen);
}
