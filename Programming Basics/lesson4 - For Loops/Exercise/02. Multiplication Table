function greet(input) {
    let n1 = Number(input[0])
    let n2 = Number(input[1])
    let total=0;
    let out=""

  for (let i = 1; i<=10; i++) {
        total=i*n1
        console.log(`${i} * ${n1} = ${total}`);
     
  }
}
