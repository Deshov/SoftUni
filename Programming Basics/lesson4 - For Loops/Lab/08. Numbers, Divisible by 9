function greet(input) {
    let n1 = Number(input[0])
    let n2 = Number(input[1])
    let total=0;
    let out=""

  for (let i = n1; i<=n2; i++) {
    if(i%9===0){
        total+=i;
        out+=i+"\n"
    }  
    
    
  }
  console.log(`The sum: ${total}`);
  console.log(out);
}
