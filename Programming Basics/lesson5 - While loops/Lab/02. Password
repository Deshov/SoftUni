function greet(input) {
    let name=input[0];
    let pass=input[1];
    let index=2

   for (let i = index; i < input.length; i++) {
       let currPass = input[i];

       if(currPass=pass){
           console.log(`Welcome ${name}!`);
           break
       }
       
   }
}
