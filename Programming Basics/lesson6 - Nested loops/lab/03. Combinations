function calc(input){
  let goal=Number(input[0]);
  let counter=0;
  for (let i = 0; i <= goal; i++) {
    for (let j = 0; j <= goal; j++) {
      for (let k = 0; k <= goal; k++) {
        if(i+j+k===goal){
          counter++;
        }
      }
    } 
  }
  console.log(counter);
}
